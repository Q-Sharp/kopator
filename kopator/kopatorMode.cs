namespace kopator
{
    public enum KopatorMode
    {
        Copy = 0,
        Collect = 1,
        Catalog = 2
    }

    public enum CatalogExportType
    {
        CSV = 1,
        HTML = 2
    }
}
